/* Copy this file to config/supabase.local.js and fill in your values
   (Supabase dashboard → Project Settings → API).
   supabase.local.js is gitignored, so the values never reach the repo.
   In production the deploy workflow writes the same file from the
   SUPABASE_URL / SUPABASE_ANON_KEY repository secrets. */
window.SUPABASE_LOCAL = {
  url: "",       // e.g. "https://abcd1234.supabase.co"
  anonKey: "",   // the "anon public" / sb_publishable_… key
};
